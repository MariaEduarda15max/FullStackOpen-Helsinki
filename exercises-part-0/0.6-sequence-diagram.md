``` sequence diagram 
    actor User
    participant Browser
    participant Server

    User ->> Browser: Writes text in input field
    User ->> Browser: Clicks "Save"
    Browser ->> JS gets the submited value
    Browser ->> update the page without reload

    Browser ->> Server: POST to https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate Server
    Server ->> Browser: HTTP 201 CREATED ({"message":"note created"})
    deactivate Server

